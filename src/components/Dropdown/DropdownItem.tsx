import React from 'react'

interface DropdownItemProps {
    onClick: () => void;
}

export const DropdownItem = (props: DropdownItemProps) => {
    return (
        <div>
            
        </div>
    )
}
